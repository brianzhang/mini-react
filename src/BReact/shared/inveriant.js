export default function inveriant(codition, format, a, b, c, d, e, f) {
  throw new Error(`
    Internal React error: invariant() is meant to be replaced at compile \n
    time. There is no runtime version.
  `)
}